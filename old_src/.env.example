# Set this only on the server or in a local .env.local file. Never expose it to the browser.
GEMINI_API_KEY=replace_with_your_key
# Optional: choose a compatible Gemini model for deployed Worker requests.
GEMINI_MODEL=gemini-2.5-flash
